// Configure your public social feed URLs here. Use RSS/Atom or JSON endpoints where available.
// Example providers: RSS feed from a blog, Twitter/X RSS proxy, LinkedIn company posts (via API), or a custom JSON feed.
// NOTE: Cross-origin (CORS) policies on social platforms may prevent direct client-side fetches.
// Consider using a small server-side proxy or an RSS-to-JSON service if needed.

export const SOCIAL_FEEDS = [
  // 'https://your-blog-or-rss-feed.example.com/feed',
  // 'https://api.rss2json.com/v1/api.json?rss_url=https://twitter.com/yourhandle',
];

export default SOCIAL_FEEDS;
